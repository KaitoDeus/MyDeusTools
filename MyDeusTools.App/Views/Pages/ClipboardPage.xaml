<Page x:Class="MyDeusTools.App.Views.Pages.ClipboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:MyDeusTools.App.Services.Impl"
      Title="Clipboard Manager"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Background="Transparent">

    <Grid Margin="40" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical">
                <TextBlock Text="{DynamicResource Clipboard_Title}" 
                           FontSize="28" 
                           FontWeight="Bold" />
                <TextBlock Text="{Binding StatusMessage}" 
                           FontSize="13" 
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                           Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" 
                        CornerRadius="4" 
                        Padding="12,6" 
                        Margin="0,0,12,0">
                    <TextBlock FontSize="13" Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                        <Run Text="Đang lưu:" />
                        <Run Text="{Binding TotalCount, Mode=OneWay}" FontWeight="SemiBold" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}" />
                        <Run Text="mục" />
                    </TextBlock>
                </Border>

                <ui:Button Content="Xóa lịch sử" 
                           Icon="{ui:SymbolIcon Symbol=Delete24}"
                           Command="{Binding ClearHistoryCommand}"
                           Appearance="Secondary"
                           Padding="16,8"/>
            </StackPanel>
        </Grid>

        <!-- Search Bar -->
        <ui:Card Grid.Row="1" Margin="0,0,0,15" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:SymbolIcon Grid.Column="0" Symbol="Search24" 
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                               Margin="8,0,10,0" 
                               VerticalAlignment="Center"/>

                <TextBox Grid.Column="1" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         FontSize="14" 
                         VerticalAlignment="Center"
                         Padding="4,2"/>

                <ui:Button Grid.Column="2" 
                           Content="Xóa tìm" 
                           Command="{Binding ClearSearchCommand}"
                           Appearance="Transparent"
                           Padding="10,4"
                           Margin="4,0,0,0"/>
            </Grid>
        </ui:Card>

        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <!-- Empty State -->
            <ui:Card HorizontalAlignment="Center" 
                     VerticalAlignment="Center" 
                     Padding="40,30" 
                     MaxWidth="460">
                <ui:Card.Style>
                    <Style TargetType="ui:Card">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasItems}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:Card.Style>
                <StackPanel HorizontalAlignment="Center">
                    <ui:SymbolIcon Symbol="ClipboardPaste24" 
                                   FontSize="48" 
                                   HorizontalAlignment="Center" 
                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                   Margin="0,0,0,15"/>
                    <TextBlock Text="Chưa có nội dung sao chép" 
                               FontSize="18" 
                               FontWeight="SemiBold" 
                               HorizontalAlignment="Center"
                               Margin="0,0,0,6"/>
                    <TextBlock Text="Khi bạn sao chép bất kỳ văn bản nào, nó sẽ tự động được lưu và hiển thị tại đây để bạn sử dụng lại nhanh chóng." 
                               TextWrapping="Wrap" 
                               TextAlignment="Center"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               FontSize="13"/>
                </StackPanel>
            </ui:Card>

            <!-- Items List -->
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" 
                          VerticalScrollBarVisibility="Auto" 
                          Background="Transparent">
                <ScrollViewer.Style>
                    <Style TargetType="ScrollViewer">
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasItems}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ScrollViewer.Style>

                <ItemsControl ItemsSource="{Binding FilteredItems}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ClipboardItemModel}">
                            <ui:Card Margin="0,0,0,12" Padding="14">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <!-- Top Bar of Item -->
                                    <Grid Grid.Row="0" Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <!-- Pinned Badge -->
                                            <Border Background="{DynamicResource AccentFillColorDefaultBrush}" 
                                                    CornerRadius="4" 
                                                    Padding="6,2" 
                                                    Margin="0,0,10,0">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsPinned}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <StackPanel Orientation="Horizontal">
                                                    <ui:SymbolIcon Symbol="Pin24" FontSize="11" Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}" Margin="0,0,4,0"/>
                                                    <TextBlock Text="ĐÃ GHIM" 
                                                               FontSize="10" 
                                                               FontWeight="Bold" 
                                                               Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Timestamp -->
                                            <TextBlock Text="{Binding CopiedAt, StringFormat='dd/MM/yyyy HH:mm:ss'}" 
                                                       FontSize="12" 
                                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                                       VerticalAlignment="Center" 
                                                       Margin="0,0,12,0"/>

                                            <!-- Char Count -->
                                            <TextBlock FontSize="12" 
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" 
                                                       VerticalAlignment="Center">
                                                <Run Text="(" />
                                                <Run Text="{Binding CharCount, Mode=OneWay}" />
                                                <Run Text="ký tự)" />
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- Actions -->
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <ui:Button Content="Sao chép lại" 
                                                       Icon="{ui:SymbolIcon Symbol=Copy24}"
                                                       Appearance="Primary"
                                                       Padding="12,5"
                                                       Margin="0,0,8,0"
                                                       Command="{Binding DataContext.CopyItemCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                       CommandParameter="{Binding}"/>

                                            <ui:Button Padding="6"
                                                       Margin="0,0,4,0"
                                                       Appearance="Transparent"
                                                       ToolTip="Ghim / Bỏ ghim"
                                                       Command="{Binding DataContext.TogglePinCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                       CommandParameter="{Binding}">
                                                <ui:SymbolIcon Symbol="Pin24" FontSize="16"/>
                                            </ui:Button>

                                            <ui:Button Padding="6"
                                                       Appearance="Transparent"
                                                       ToolTip="Xóa mục này"
                                                       Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                       CommandParameter="{Binding}">
                                                <ui:SymbolIcon Symbol="Delete24" FontSize="16" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                                            </ui:Button>
                                        </StackPanel>
                                    </Grid>

                                    <!-- Content Preview -->
                                    <Border Grid.Row="1" 
                                            Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" 
                                            CornerRadius="6" 
                                            Padding="10">
                                        <TextBox Text="{Binding Content, Mode=OneWay}"
                                                 IsReadOnly="True"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 TextWrapping="Wrap"
                                                 MaxHeight="120"
                                                 VerticalScrollBarVisibility="Auto"
                                                 FontSize="13"
                                                 FontFamily="Consolas, Segoe UI, sans-serif"/>
                                    </Border>
                                </Grid>
                            </ui:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
