<Page x:Class="MyDeusTools.App.Views.Pages.StickyNotePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:models="clr-namespace:MyDeusTools.App.Services.Impl"
      Title="Sticky Note"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Stretch" Margin="0,0,0,20">
            <TextBlock Text="Sticky Notes" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       VerticalAlignment="Center"/>
            
            <ui:Button Content="Thêm ghi chú" 
                       Icon="{ui:SymbolIcon Symbol=Add24}"
                       Command="{Binding AddNoteCommand}"
                       Appearance="Primary"
                       Margin="20,0,0,0"
                       Padding="15,8"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Notes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:StickyNoteModel}">
                        <ui:Card Margin="0,0,15,15" Width="250" Height="250" Padding="0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                                         AcceptsReturn="True"
                                         TextWrapping="Wrap"
                                         Background="Transparent"
                                         BorderThickness="0"
                                         Padding="15"
                                         FontSize="14"
                                         VerticalAlignment="Stretch"
                                         LostFocus="OnTextBoxLostFocus">
                                    <TextBox.InputBindings>
                                        <KeyBinding Key="Enter" Command="{Binding DataContext.AutoSaveCommand, RelativeSource={RelativeSource AncestorType=Page}}" />
                                    </TextBox.InputBindings>
                                </TextBox>

                                <Border Grid.Row="1" Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}" Padding="10,5">
                                    <Grid>
                                        <TextBlock Text="{Binding CreatedAt, StringFormat='dd/MM/yyyy HH:mm'}" 
                                                   FontSize="10" 
                                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                   VerticalAlignment="Center"/>
                                        
                                        <ui:Button HorizontalAlignment="Right"
                                                   Padding="4"
                                                   Appearance="Transparent"
                                                   Command="{Binding DataContext.DeleteNoteCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                                   CommandParameter="{Binding}">
                                            <ui:SymbolIcon Symbol="Delete24" FontSize="16" Foreground="{DynamicResource SystemFillColorCriticalBrush}"/>
                                        </ui:Button>
                                    </Grid>
                                </Border>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
